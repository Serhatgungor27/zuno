# Copy to .env.local and fill in. Both values are public client keys — the anon
# key is safe in the bundle because RLS is enforced server-side. Never put the
# service-role key here; EXPO_PUBLIC_* is embedded in the shipped app.
EXPO_PUBLIC_SUPABASE_URL=https://fhitxtzgjarffixuvicv.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

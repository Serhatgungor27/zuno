# expo-router pulls react-dom@19.2.8 for its web build, which wants react ^19.2.8,
# while Expo SDK 57 pins react 19.2.3. `npx expo install` passes --legacy-peer-deps
# for exactly this reason; make a plain `npm install` behave the same way.
legacy-peer-deps=true

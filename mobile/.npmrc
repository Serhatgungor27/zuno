# expo-router pulls react-dom for its web build, which wants a newer react than
# Expo pins. `npx expo install` passes --legacy-peer-deps for this; make plain
# `npm install` behave the same.
legacy-peer-deps=true

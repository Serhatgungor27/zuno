import { Placeholder } from "../../components/Placeholder";

export default function Notifications() {
  return (
    <Placeholder
      title="Activity"
      note="Next: wire to /api/notifications. Blocked until the backend accepts a bearer token — it reads the signed Spotify cookie today."
    />
  );
}

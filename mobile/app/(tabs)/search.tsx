import { Placeholder } from "../../components/Placeholder";

export default function Search() {
  return (
    <Placeholder
      title="Search"
      note="Next: wire to /api/search. That route is already input-sanitised server-side via lib/pgrest.ts."
    />
  );
}
